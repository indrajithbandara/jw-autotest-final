共同参数：plf_cmm_parameter, plf_cmm_param_multilang（PCM、GRP、PMS）
集团参数：cmm_base_param, cmm_base_param_multilang（PCM、GRP、PMS）
酒店参数：cmm_unit_param, cmm_unit_param_multilang（PCM、GRP、PMS）
pos参数： cmm_pos_param, cmm_pos_param_multilang（GRP、PMS）
系统参数：plf_cmm_config, plf_cmm_config_multilang（PCM、GRP、PMS）

多项表：cmm_multi_item_param（PCM、GRP、PMS）
发番定义表：cmm_number（PCM、GRP、PMS）
开关表：grp_cmm_option（GRP）


集团：plf_cis_chain, （PCM）
酒店：plf_cis_unit, （PCM）
数据库：plf_ops_dbconf, （PCM）
数据库：plf_ops_dbinfo（PCM）
菜单：plf_pdt_menu（PCM）
权限：plf_pdt_permission（PCM）
权限：plf_pdt_permstyp（PCM）

grp_cmm_department（GRP）
grp_cmm_empl_role（GRP）
grp_cmm_employee（GRP）
grp_cmm_role（GRP）
grp_cmm_role_permission（GRP）
grp_cmm_saler（GRP）
grp_cmm_unit（GRP）
grp_cmm_unit_org（GRP）
grp_cmm_user（GRP）
grp_cmm_user_menu（GRP）
grp_cmm_user_role（GRP）